# 7.10 — Smoke Test Implementation

## Description

Prompt to generate an executable smoke test script from the checklist defined in `07-04`. Produces an automatable artifact that validates in minutes that the system is operational after a deployment.

**When to use:** after completing smoke test design (`07-04`), as an immediate post-deployment step to decide whether the environment is ready for full testing or requires rollback.

---

## Editorial Contract

| Field | Value |
|---|---|
| Type | execution |
| Expected risk | medium — the script must not generate or modify business data, but its result (PASS/FAIL) can determine whether a rollback is executed |
| Required inputs | smoke test checklist generated by `07-04`, Test stack profile from `07-00`, documented health endpoints or critical flows |
| Allowed tools | reading the checklist and project stack, writing the script in the workspace/working branch, running the script only against read-only checks (health checks, verification flows) — no mutating business data or running against production without authorization |
| Permitted autonomy | A2 — Execute controlled |
| Stop criteria | if the `07-04` checklist does not exist or is incomplete, or if any check would require writing or modifying business data to verify itself, stop and escalate instead of improvising the check |
| Expected output | see `## Expected output` |
| Minimum evidence | the script must have run at least once locally or in CI, showing the PASS/FAIL summary with the corresponding exit code |
| Recommended next prompt | `09-04-promotion-checklist` to integrate the smoke test result as a post-deployment validation |

---

## Mandatory previous context

> Include the block from `00-framework.md` before this prompt.
> Attach the **Test stack profile** generated by `07-00` (run it once per project if it does not exist yet).
> Attach or reference the smoke test checklist generated by `07-04`.

---

## Complete prompt

```text
Objective:
Implement the smoke tests defined in the attached checklist as an automatable script
that validates system health in under 15 minutes.

Steps:
1. Evaluate which form of automation is most appropriate for the project:
   - API/HTTP script: if checks are health endpoint or API calls,
   - UI script: if it requires interface interaction (use the available E2E framework),
   - shell script: if it validates processes, services, or system-level connectivity,
   - combination of the above if the system is mixed.
2. For each checklist item, implement the corresponding verification:
   - clear and verifiable success condition,
   - readable result message: PASS / FAIL + error detail on failure,
   - maximum wait time per verification.
3. Organize checks in order of criticality: most blocking ones first.
4. Implement a final summary: total checks, passed, failed, total time.
5. The script must return exit code 0 if all pass, non-zero if any fail
   (for CI/CD pipeline integration).

Constraints:
- The complete script must run in under 15 minutes.
- Must not generate or modify business data in production.
- Must be executable without manual intervention from command line or pipeline.

Deliverables:
- executable smoke test script,
- usage instructions (required environment variables, how to run it),
- suggested integration with the project's CI/CD pipeline.
```

---

## Standard formula usage

```text
Use the smoke test implementation prompt and adapt it to:
- repository: [NAME OR URL]
- deployed branch or version: [REFERENCE]
- environment: [QA / STAGING / PROD]
- preferred script type: [AUTO-DETECT / API / UI / Shell / Mixed]
- available E2E framework: [Playwright / Cypress / none / etc.]
- smoke checklist: [ATTACH OR REFERENCE THE 07-04 DOCUMENT]
- documents to review: available health endpoints, documented critical flows
- specific output goal: pipeline-executable script that runs in < 15 minutes
- depth level: low
```

---

## Expected output

A self-contained script, for example:

```
scripts/smoke-test.[sh|py|js|ts]
tests/smoke/smoke.spec.[ext]
```

With console output like:

```
[SMOKE TEST] Starting verification — environment: staging
  [PASS] Login / authentication (1.2s)
  [PASS] Access to main module (0.8s)
  [PASS] Critical basic operation (2.1s)
  [FAIL] Integration with service X — timeout after 10s
  [PASS] No visible errors in UI (1.5s)

Result: 4/5 checks passed — FAILURE (exit code 1)
Total time: 5.6s
```
